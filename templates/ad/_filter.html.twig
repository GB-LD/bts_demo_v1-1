{% form_theme formView 'form/_tailwindFormTheme.html.twig' %}

{{ form_start(formView) }}
    {{ form_row(formView.q) }}
    <div class="border border-bts-green-400">
        {{ form_row(formView.categories, {attr :{class:'flex flex-col'}}) }}
    </div>

    <button type="submit" class=" px-6 py-2  transition ease-in duration-200 uppercase rounded-full hover:bg-gray-800 hover:text-white border-2 border-gray-900 focus:outline-none">
        Filtrer
    </button>

{{ form_end(formView) }}